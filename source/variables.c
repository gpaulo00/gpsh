#include <main.h>

//~ int set_variable(char *name, char *value){
    //~ if(vars->size>vars->used){
        //~ vars->used++;
    //~ } else {
        //~ char **aux, **aux2;
        //~ aux = realloc(vars->names, INCREMENT * sizeof(char *));
        //~ aux2 = realloc(vars->values, INCREMENT * sizeof(char *));
        //~ if(aux==NULL||aux2==NULL){printf(RED "Error allocating dynamic memory\n"); closeApp=true; return 2;}
        
        //~ vars->size += INCREMENT;
        //~ vars->used++;
        //~ vars->names = aux;
        //~ vars->values = aux;
    //~ }
    //~ vars->names[vars->used] = name;
    //~ vars->values[vars->used] = value;
    //~ return 0;
//~ }
